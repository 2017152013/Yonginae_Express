<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('header') %>
    <link href = "https://fonts.googleapis.com/css2?family=Black+Han+Sans&display=swap" rel="stylesheet">
    <link href = "https://fonts.googleapis.com/css2?family=Do+Hyeon&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/mypage.css">

</head>
<body>
    <%- include('nav') %>
    <main>
        <h1><%=user%>님의 챌린지 기록 확인</h1>
        <% if (challs.length == 0) {%>
            <p style="text-align: center;">챌린지 기록이 없습니다!
            <a href="/challenge/review_select">첫 챌린지 참여하기</a></p>
        <%} else{%>
            <div class="reviewContent">
                <% for (chall of challs){%>
                <div class="review-container">
                    <div>
                        <p>👏:<%= chall.claps %></p>
                      <div class="img-container">
                        <img src="/uploads/<%= chall.review.filename %>" alt="review image">
                      </div>
                      <a href="/review/detail/<%= chall.review._id %>"><button>리뷰 보러가기</button></a>
                    </div>
                </div>
                <%}%>
              </div>
        <%}%>
    </main>
    
</body>
</html>