<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('header') %>
    <link rel="stylesheet" href="/css/review.css">
    <link href = "https://fonts.googleapis.com/css2?family=Black+Han+Sans&display=swap" rel="stylesheet">
    <link href = "https://fonts.googleapis.com/css2?family=Do+Hyeon&display=swap" rel="stylesheet">

</head>
<body>
    <%- include('nav') %>
    <main>
        <% if (reviews.length > 0) {%>
            <h1><%= place_name %> 리뷰</h1>
            <div class = "reviewBtn">
                <% if (is_logined) { %>
                    <a href="/place/place_create/<%=id%>/<%=place_name%>">이 식당에 대한 리뷰 작성</a>
                <% } else {%>
                    <a href="/account/login">리뷰 작성</a>
                <% } %>
            </div>
            <div class ="reviewContent">
                <% reviews.forEach(function(review){%>
                    <a href="/review/detail/<%= review.id %>">
                        <div>
                            <img src="http://localhost:3000/uploads/<%= review.filename %>" alt="review image" width="400" height="400">
                            <h3>제목: <%= review.title %></h3>
                            <p>작성자: <%= review.writer %></p>
                        </div>
                    </a>
                <%})%>
            </div>
        <%} else{%>
            <h1><%= place_name %> 리뷰가 아직 등록되지 않았습니다.</h1>
            <h2>소중한 첫 번째 리뷰를 작성해 주세요!</h2>
            <div class = "reviewBtn">
                <% if (is_logined) { %>
                    <a href="/place/place_create/<%=id%>/<%=place_name%>">이 식당에 대한 리뷰 작성</a>
                <% } else {%>
                    <a href="/account/login">리뷰 작성</a>
                <% } %>
            </div>
        <%}%>
        
    </main>
</body>
</html>